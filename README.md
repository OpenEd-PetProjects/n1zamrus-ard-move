# n1zamrus-ard-move
Пет-проект «Датчик движения на ардуино» — учебный репозиторий Open-ED

## Проверка

Логика определения движения вынесена в отдельную чистую Python-функцию, чтобы её
можно было проверять в CI без подключённой платы Arduino. Запустите тесты командой:

```bash
make test
```
